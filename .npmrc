# This prevents peer dependency conflicts from failing installations (ERESOLVE errors)
# It mirrors passing --legacy-peer-deps on the command line.
legacy-peer-deps=true

# Optional: reduce audit verbosity in CI logs if you prefer
# audit=false

# Optional: prefer using the lockfile in CI
# prefer-offline=false
